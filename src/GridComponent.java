public interface GridComponent { //represents our GridEnum and our GridCar so we could store an enum and a class object in our grid data structure
	int rowLocation = 0;
	int columnLocation = 0;
	
	public String print();
}
